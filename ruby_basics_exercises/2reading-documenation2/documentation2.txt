1.Methods Without Arguments
"xyz".upcase
--------------------------------------------
2 a[2].insert(3,5,6,7)
--------------------------------------------
3 the split without and argument split on empty space by default
the argument will delete out the given argument and split the string at that point.
if a second argument is given it controls how many elements are created in the new array.
the split method returns the string as an array of strings.
--------------------------------------------
